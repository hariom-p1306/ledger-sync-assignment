services:
  mongo:
    image: mongo:8.0
    container_name: ledger-sync-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ledger_sync
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
